name: Validate Terraform

on:
  push:
    branches: [main]
    paths:
      - 'blueprints/**'
      - '.github/workflows/validate.yml'
      - '.github/scripts/validate-blueprint.sh'
  pull_request:
    branches: [main]
    paths:
      - 'blueprints/**'
      - '.github/workflows/validate.yml'
      - '.github/scripts/validate-blueprint.sh'

jobs:
  detect-changes:
    name: Detect Changed Blueprints
    runs-on: ubuntu-latest
    outputs:
      blueprints: ${{ steps.changes.outputs.blueprints }}
      has_changes: ${{ steps.changes.outputs.has_changes }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get changed blueprints
        id: changes
        run: |
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            BASE_SHA=${{ github.event.pull_request.base.sha }}
            HEAD_SHA=${{ github.event.pull_request.head.sha }}
          else
            BASE_SHA=${{ github.event.before }}
            HEAD_SHA=${{ github.sha }}
          fi

          # Get list of changed blueprint directories
          CHANGED_DIRS=$(git diff --name-only $BASE_SHA $HEAD_SHA | \
            grep '^blueprints/' | \
            grep -v '^blueprints/_template/' | \
            cut -d'/' -f1-2 | \
            sort -u | \
            jq -R -s -c 'split("\n") | map(select(length > 0))')

          echo "Changed directories: $CHANGED_DIRS"
          echo "blueprints=$CHANGED_DIRS" >> $GITHUB_OUTPUT

          if [ "$CHANGED_DIRS" != "[]" ] && [ "$CHANGED_DIRS" != "" ]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi

  terraform-fmt:
    name: Terraform Format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.7.0"

      - name: Terraform Format Check
        run: terraform fmt -check -recursive blueprints/

  blueprint-validate:
    name: Blueprint Validate
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.has_changes == 'true'
    strategy:
      fail-fast: false
      matrix:
        blueprint: ${{ fromJson(needs.detect-changes.outputs.blueprints) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.7.0"

      - name: Run blueprint validation gate
        run: .github/scripts/validate-blueprint.sh "${{ matrix.blueprint }}"

  template-validate:
    name: Validate Template
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.7.0"

      - name: Terraform Init (Template)
        working-directory: blueprints/_template
        run: terraform init -backend=false

      - name: Terraform Validate (Template)
        working-directory: blueprints/_template
        run: terraform validate

  summary:
    name: Validation Summary
    runs-on: ubuntu-latest
    needs: [terraform-fmt, blueprint-validate, template-validate]
    if: always()
    steps:
      - name: Check results
        run: |
          if [ "${{ needs.terraform-fmt.result }}" == "failure" ] || \
             [ "${{ needs.blueprint-validate.result }}" == "failure" ] || \
             [ "${{ needs.template-validate.result }}" == "failure" ]; then
            echo "One or more validation checks failed"
            exit 1
          fi
          echo "All validation checks passed!"
